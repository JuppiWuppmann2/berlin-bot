services:
  - type: web
    name: berlin-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BSKY_HANDLE
        sync: false   # trägst du im Render Dashboard ein
      - key: BSKY_PASSWORD
        sync: false   # auch im Dashboard eintragen
